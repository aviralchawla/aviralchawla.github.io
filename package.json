{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "build": "observable build",
    "dev": "observable preview",
    "deploy": "rimraf node_modules/.cache/gh-pages && npm run build && gh-pages -d dist -b main",
    "observable": "observable"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.3",
    "d3-dsv": "^3.0.1",
    "d3-time-format": "^4.1.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "rimraf": "^5.0.10"
  },
  "engines": {
    "node": ">=18"
  }
}
